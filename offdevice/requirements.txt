# Off-device feature pipeline.
#
# librosa drives the feature numbers, so its EXACT resolved version is part of the
# contract -- record `pip freeze` for librosa/numpy/scipy to pin a known reference.
numpy>=1.26,<2.1
scipy>=1.11
librosa>=0.10.2,<0.11
scikit-learn>=1.3
soundfile>=0.12
pytest>=8.0

# Serial capture of NS-flash dumps from the board (offdevice/data/capture.py): pyserial
# drives USART1 @ 921600 8N1 to read the framed 256 KB dump. Pure-python and tiny, so it
# doesn't perturb the librosa/numpy/scipy versions that define the feature contract.
pyserial>=3.5

# Anomaly-eval report figures only (offdevice/eval/report_plots.py). Rendering, never
# math: no feature or model number depends on it, so it sits outside the pinned
# librosa/numpy/scipy contract.
matplotlib>=3.8
